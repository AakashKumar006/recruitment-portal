spring:
  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    password:
    url: jdbc:h2:mem:vwits_recruitment_portal
    username: sa
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
